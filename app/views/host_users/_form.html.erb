<%= form_for(@host_user) do |f| %>
  <% if @host_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@host_user.errors.count, "error") %> prohibited this host_user from being saved:</h2>

      <ul>
      <% @host_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :activity %><br>
    <%= f.text_field :activity %>
  </div>
  <div class="field">
    <%= f.label :old_visit %><br>
    <%= f.check_box :old_visit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
