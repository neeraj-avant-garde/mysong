<%= form_for(@playlist_song) do |f| %>
  <% if @playlist_song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playlist_song.errors.count, "error") %> prohibited this playlist_song from being saved:</h2>

      <ul>
      <% @playlist_song.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :playlist_id %><br>
    <%= f.number_field :playlist_id %>
  </div>
  <div class="field">
    <%= f.label :song_id %><br>
    <%= f.number_field :song_id %>
  </div>
  <div class="field">
    <%= f.label :rank %><br>
    <%= f.number_field :rank %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
